# General Knowledge Project

This is a general knowledge application / quiz project.

## Project Structure
- Contains HTML/JS/CSS for the frontend interface.
- Includes Python scripts to fetch and parse questions.

## Setup
1. Clone the repository
2. Install Python dependencies if running the scripts: `pip install -r requirements.txt`
3. Serve the HTML files locally or deploy to a web server.
